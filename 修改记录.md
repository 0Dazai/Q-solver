# 修改记录

## 2026-09-05：实时面试链路效率与连续多轮可靠性优化（glm/interview-improvements）

本轮目标不是扩展功能，而是提高面试模式真实使用中的响应效率与连续多轮可靠性。

### P0-1 聚合器多轮可靠性（pkg/interview/aggregator.go）

1. 新增短追问识别：面试中段（存在上一题时），"Redis呢""线程池呢""GC了解吧""项目里呢"等省略句、无问号短追问可稳定提交；首题仍走严格意图门控，环境噪声、纯应答词（"好的""嗯"）不触发。
2. 区分"没有问题"与"问题很短"：新增纯填充语过滤与结尾语气词（呢/吧/吗/么）、追问内容词、技术词三层判定。
3. 修复 partial 回声：重复相同的 partial 不再刷新稳定时钟，partial-only 文本最终一定可推进提交。
4. 修复 turn 污染：新 final 到来时，稳定窗口已过且不可提交的残留文本（回声、填充语、无意图）被丢弃，不再拼接进下一题；可提交但被本地讲话抑制的问题会延迟提交而不是丢失。
5. 手动提交不再受意图门控限制（用户显式编辑后短追问可直接提交）。

### P0-2 回答请求超时与槽位释放（pkg/interview/answer.go、answer_coordinator.go）

1. 新增集中定义的超时常量：首 Token watchdog 默认 8s、SSE 空闲 watchdog 默认 15s、总时长口语默认 30s、深入分析 90s；HTTPAnswerExecutor 支持覆盖字段。
2. 卡死请求不再永久占用并发槽：watchdog 触发后取消请求并返回脱敏错误（"首 Token 超时/SSE 空闲超时/总回答超时，已释放回答通道"）。
3. 保留原调度语义：双并发、已开始的健康旧回答不被新问题取消、pending 队列偏向最新、防饥饿轮转不变；用户取消仍映射为 context.Canceled（"已停止生成"）。
4. 模型流中断时保留已生成内容并标记 error，可重新提交；所有任务保证终态（final/error/cancelled）。

### P0-3 主链路去阻塞

1. 检索日志改为异步有界队列（满则丢弃），日志失败不再阻塞提交与回答路径。
2. 面试官问题 timeline 事件在检索前即发出；SQLite/Markdown 持久化维持异步队列（审查确认未阻塞模型入队）。

### 端到端延迟指标（pkg/interview/latency.go，新增）

每题记录：ASR 时间戳、聚合等待、检索、上下文构建、排队等待、请求执行、入队→首片段、片段数、端到端总耗时与结果（completed/error/canceled/timeout），写入 retrieval.log；内容脱敏（不含 API Key、简历、完整提示词、资料正文）。

### 回答效率提示词（pkg/interview/prompt.go）

首句直接给核心答案（前 80 字内出现结论要点），禁止无信息开场；简单事实题短答；保持候选人口语、第一人称、不编造、不反问等既有约束；提示词保持紧凑预算。

### 工程补强

1. 更新设计文档中"新问题取消旧回答"的过期描述为实际调度语义。
2. PDF 工具查找去掉固定 D:\msys64 依赖：PATH → Q_SOLVER_PDF_TOOLS 环境变量 → MSYS2 环境变量与常见默认目录。
3. .gitignore 补充 *.db/*.sqlite*/面试记录目录/OCR 临时目录/本地模型文件等兜底规则。
4. README 与 README_EN 仓库地址、badge、clone 地址改为本仓库，Go 版本与 go.mod 统一为 1.24+。
5. npm audit 非破坏性修复（nanoid、brace-expansion）；esbuild/vite 升级需大版本，另行评估。

### 验证

- go test -race -tags sqlite_fts5 ./... 全部通过（含新增聚合器、超时、协调器恢复、延迟指标测试）。
- 前端 Vitest 11/11 通过，Vite 生产构建通过，Wails 正式构建通过。
- 真实 API（千问 qwen-flash，DashScope 兼容接口）：单请求 TTFT 160ms/总 541ms；双并发经 AnswerCoordinator 均完成；强制超时后槽位正确释放、后续请求恢复。

## 2026-06-21：同步本地修改版到 GitHub fork

本次变更将本地修改后的 Q-Solver 源码同步到 `0Dazai/Q-solver` 仓库，用于保留当前可运行版本和后续继续维护。

## 主要修改

1. 重构桌面应用后端结构
   - 将原先集中在根目录 `app.go` 的逻辑拆分为 `app/` 目录下多个文件。
   - 新增或同步 `app/app.go`、`app/llm.go`、`app/settings.go`、`app/solve.go`、`app/window.go`、`app/screen.go`、`app/shortcut.go`、`app/resume.go`、`app/misc.go`。
   - 目的：让模型、设置、解题、窗口、截图、快捷键等入口更清晰。

2. 增加模型 Provider 和豆包 / 火山方舟兼容支持
   - 配置中增加 `provider`、`baseURL`、`model`、`prompt` 等字段。
   - 支持 OpenAI 兼容接口、DeepSeek 文本模型、豆包 / 火山方舟视觉模型。
   - 示例配置见 `config/config.example.json`。

3. 增强 LLM 请求能力边界
   - DeepSeek 保留纯文本能力。
   - 图片或 PDF 请求会在发送前进行能力判断，避免把 `image_url` 发给不支持视觉输入的模型。
   - 新增 `pkg/llm/openai_test.go` 覆盖关键 LLM 请求行为。

4. 支持手动填写模型 ID / 接入点 ID
   - 前端模型设置支持手动输入模型 ID 或火山方舟 `ep-...` 接入点 ID。
   - 适配火山方舟控制台中模型权限、地域、项目不一致时的真实使用场景。

5. 增加用户附加要求
   - 设置页支持填写“附加要求”。
   - 附加要求会注入解题提示，用于控制编程题默认语言等偏好。
   - 避免把默认语言硬编码为 Python、Java 或其他固定语言。

6. 调整窗口、最小化和隐身能力边界
   - 普通最小化保持任务栏可恢复。
   - F9 隐身能力继续与 Windows display affinity 相关逻辑保持独立。
   - 相关代码位于 `pkg/state`、`pkg/platform`、`pkg/shortcut` 和 `app/window.go`。

7. 重构前端状态和组件
   - 新增 `frontend/src/stores/settings.js`、`frontend/src/stores/solution.js`、`frontend/src/stores/ui.js`。
   - 新增 `frontend/src/services/api.js`、`events.js`、`theme.js`。
   - 新增 `DomainSelector`、`ScreenshotDock`、`SolveView`、`ThemeToggle`、`ThinkingBlock` 等组件。
   - 设置页恢复分类 prompt 展示，并保留“附加要求”配置入口。

8. 新增工具化截图模块
   - 新增 `pkg/tools/registry.go`、`pkg/tools/screenshot.go`、`pkg/tools/types.go`。
   - 为后续工具化截图和模型调用扩展提供统一入口。

9. 清理旧结构和生成物
   - 删除旧根目录 `app.go`。
   - 删除旧的 live/audio/Claude/Gemini 等未保留模块。
   - 删除 `.DS_Store`、`.idea` 等不应继续跟踪的本地文件。
   - `.gitignore` 更新为忽略构建产物、本地配置、依赖目录和缓存。

## 新增文件示例

- `app/app.go`
- `app/solve.go`
- `app/window.go`
- `config/config.example.json`
- `frontend/src/components/DomainSelector.vue`
- `frontend/src/components/ScreenshotDock.vue`
- `frontend/src/components/SolveView.vue`
- `frontend/src/services/api.js`
- `frontend/src/stores/settings.js`
- `frontend/src/stores/solution.js`
- `frontend/src/stores/ui.js`
- `pkg/common/constants.go`
- `pkg/config/crypto.go`
- `pkg/domain/prompts.go`
- `pkg/llm/openai_test.go`
- `pkg/tools/registry.go`
- `pkg/tools/screenshot.go`
- `pkg/tools/types.go`

## 验证结果

已在本地执行：

```powershell
go test ./pkg/llm
```

结果：

```text
ok Q-Solver/pkg/llm
```

说明：本次仅自动验证了 LLM 相关测试。窗口最小化、F9 隐身、截图识别、真实豆包 / 火山方舟调用仍需要在 Windows 桌面环境中人工复测。

## 回滚方式

如果需要回滚 GitHub 仓库，可在 GitHub 或本地 Git 中回退本次提交，恢复到提交前的 `main` 分支状态。

